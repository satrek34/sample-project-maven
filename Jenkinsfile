pipeline {
    agent any
    options {
        timeout(time: 02, unit: 'MINUTES') 
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
